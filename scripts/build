#!/bin/sh

set -eu

ROOT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"

DISTRO="${1:-}"

case "$DISTRO" in
    debian)
        exec sh "$ROOT_DIR/distros/debian/build.sh"
        ;;
    *)
        echo "Usage: $0 debian" >&2
        exit 1
        ;;
esac
